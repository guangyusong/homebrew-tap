# Homebrew Tap

Homebrew formulas for small tools by `guangyusong`.

## ropen

```bash
brew tap guangyusong/tap
brew install ropen
ropen doctor
ropen iterm install
```

`ropen` opens remote terminal paths locally on macOS.
